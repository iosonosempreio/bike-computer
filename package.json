{
  "name": "application-name",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "bike-computer": "git fetch --all && npm install && npm start"
  },
  "dependencies": {
    "bootstrap": "^5.1.0",
    "express": "^4.17.1",
    "termux-api": "0.0.1"
  }
}
